using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileInfo : MonoBehaviour
{
    public int tileIndex;
    public string actionDescription = "로딩 전..."; // 기본값 혹은 로딩 실패 시 표시될 값

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
